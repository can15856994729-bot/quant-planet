<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        主题：无 ActionBar，状态栏和导航栏透明
        配合 viewport-fit=cover + Capacitor edge-to-edge，WebView 延伸到系统栏下方。
        CSS 通过 env(safe-area-inset-top/bottom) 获取安全区高度，页面自行处理内边距。
        minSdkVersion=24，透明状态栏 / windowLightStatusBar 均支持。
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- 状态栏和导航栏透明，WebView 绘制区域覆盖整个屏幕 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- 浅色图标（白色），适配深色背景 #07111F；false = 白色状态栏图标 -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>

</resources>
